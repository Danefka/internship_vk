# vk_internship
# VK Internship Test Task  My solution for the VK internship test assignment.  A Java gRPC key-value service with `put`, `get`, `delete`, `range`, and `count` operations.   Data is stored in Tarantool 3.2.x, with support for up to 5,000,000 records and `null` values in the `value` field.
